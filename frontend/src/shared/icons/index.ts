/**
 * Icon adapters — Phosphor SVG icons (tree-shakeable).
 *
 * Evidence: Shopify ships `@shopify/polaris-icons`; Meta/Fluent ship
 * design-system icon packages. We use Phosphor as a maintained SVG set
 * rather than hand-rolled paths (Master Framework asset/UI architecture).
 */
export {
  Moon as MoonIcon,
  Sun as SunIcon,
  SpinnerGap as SpinnerIcon,
  SignOut as SignOutIcon,
  Devices as DevicesIcon,
  ArrowLeft as ArrowLeftIcon,
  WarningCircle as WarningIcon,
  CheckCircle as CheckCircleIcon,
  EnvelopeSimple as EnvelopeIcon,
} from "@phosphor-icons/react";
